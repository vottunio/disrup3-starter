const {vottunSdk} = require('../config.js');

async function deployPoapContract() {
   // Implement SDK call that deploys the POAP contract
}

deployPoapContract().then((result) => {
    console.log(result)
});