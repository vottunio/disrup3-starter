const {vottunSdk} = require('../config.js');

async function uploadZip() {
   // Implement SDK call that uploads POAP metadata (JSON file stored in a zipped directory) to IPFS
}

uploadZip().then((result) => {
    console.log(result)
});

