const {vottunSdk} = require('../config.js');

async function uploadImage() {
   // Implement SDK call that uploads the POAP image to IPFS
}

uploadImage().then((image) => {
    console.log(image)
});

